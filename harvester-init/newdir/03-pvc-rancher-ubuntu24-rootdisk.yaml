apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rancher-ubuntu24-rootdisk
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: longhorn
